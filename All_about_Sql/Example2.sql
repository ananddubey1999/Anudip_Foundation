create database Example1Country;
use Example1Country;
CREATE TABLE `Name` (`COUNTRY_ID` varchar(50), `COUNTRY_NAME` varchar(50), `REGION_ID` int);
INSERT INTO Name VALUES ('C2', 'United States', 1002);
select * from Name;

